// Package main is a placeholder. This repository is a miniature stand-in for
// github.com/apache/skywalking-banyandb, used by vajra's tdd-bydb e2e suite so
// its gates run a real `go build`/`go vet`/`make` without paying banyandb's own
// build cost. There is nothing else here on purpose.
package main

func main() {}
